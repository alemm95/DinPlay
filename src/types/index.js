// Archivo para definir tipos TypeScript (opcional)
// Si decides usar TypeScript en el futuro, aquí puedes definir los tipos

/*
// Ejemplo de tipos para TypeScript:

export interface User {
  id: number;
  name: string;
  email: string;
  avatar?: string;
}

export interface AuthState {
  user: User | null;
  isAuthenticated: boolean;
  loading: boolean;
  error: string | null;
}

export interface ApiResponse<T> {
  data: T;
  status: number;
  message?: string;
}

export interface LoginCredentials {
  email: string;
  password: string;
}

export interface RegisterData {
  name: string;
  email: string;
  password: string;
  confirmPassword: string;
}

export type ButtonVariant = 'primary' | 'secondary' | 'outline';
export type ButtonSize = 'small' | 'medium' | 'large';

*/

// Por ahora, este archivo sirve como placeholder para futuros tipos
export default {};
